---
name: plainify
description: Claude Code / Codex などが出力したコードレビュー・解析の難解で冗長な文章を平易な文章へ整形する（ユーザー起動専用の事後リライト。--for <読者> で読者適応）。
disable-model-invocation: true
argument-hint: "[--for 読者] [テキスト / @path / PR・issue の URL か #番号]"
---

# plainify — AI 生成文の明快化リライト

> **明示起動の確認**: ユーザーがこのスキルを名指しで起動した場合のみ実行する。
> 文脈からの自動適用で読み込まれた場合は、実行せずユーザーに起動意思を確認する
> （`disable-model-invocation` を解釈しない実行環境での暴発防止）。
> 例外: 他のスキルが本 SKILL.md を Read して規則を同一コンテキストで適用する参照利用は、
> 参照元スキルの起動判断に従うため、この確認は不要。

AI コーディングエージェント（Claude Code / Codex など）が出力するコードレビュー・
解析結果の「難解・冗長な文章」を、読みやすい文章へ整形する。
これは **事後リライト専用**。生成時の予防（出力スタイル注入）はしない。

モードは `--for` の有無で二値に分岐する:

- **既定モード（意味厳守）**: 意味を 1 つも変えずに、原文と同じ言語のまま、
  すらすら読める平易で自然な文章へ整形する。読者は原文と同じ技術者。規則は「手順」の節。
- **`--for <読者>`（読者適応）**: 読者を指定された読者に置き換え、事実を厳守したまま
  詳細度を読者に合わせて調整する。読者は自由記述で指定する（例: `--for クライアント`、
  `--for 経営層`、`--for 新人エンジニア`）。規則は「--for モード」の節
  （既定モードとの差分のみ規定）。

<!-- BEGIN PROSE-PRESENTATION-CANONICAL -->
**散文提示**: 本スキルでユーザーに選択・確認を求めるときは、選択肢（または一覧）を空行で区切って番号を付け、各項目は「現状どうなっているか → 採用・回答すると何がどう変わるか」が再読なしに分かる完結した文で書き（本スキル内部の言葉にはその場で一言の説明を添える）、推奨する選択肢を理由一言つきで明示し（推奨できる選択肢が無い場合は「推奨なし」とその理由を明示する）、ツールを呼ばずターンを終了してテキスト回答を待つ。定型の選択肢でどれも一行で意味が通る場合に限り、空行区切りにせず一行に畳んでよい（推奨案と理由の明示は維持する）。複数項目の一覧は、1項目を「見出し行（識別子と主情報）＋属性ごとの下位箇条書き」の1ブロックで書き、1行に1属性とする（Markdown は単一改行を結合するため、改行は箇条書き構造または空行で確保する）。以下で「散文提示」はこの形式を指す。
<!-- END PROSE-PRESENTATION-CANONICAL -->

## このスキルの責務と割り切り

- **やること:** 分かりやすさ（明快さ・簡潔さ・自然さ）の改善だけ。
- **やらないこと:** 正しさの検証はしない。原文に技術的・事実的な誤りがあっても、
  指摘せず、そのまま整形する（＝誤った指摘を読みやすく磨いてしまう副作用は
  許容する。正しさの検証は別の責務）。
- **呼び出し方:** ユーザーが直接 `/plainify` を実行するのが基本。参照利用の可否と
  モデル自動起動の禁止（暴発防止）は冒頭の「明示起動の確認」に従う。参照利用を
  別プロセス化しないのは、事後リライトでは対象テキストが呼び出し元の手元にあるためである。

## 言語の扱い（普遍規則＋言語別規則）

- 本 SKILL.md の規則はすべて言語に依存しない**普遍規則**であり、対象テキストの言語を
  問わず適用する。整形は対象テキストの言語のまま行う（翻訳はしない）。
- 整形前に対象テキストの**主たる言語**（分量の最も多い言語）を判定し、本スキルの
  ディレクトリ直下の `rules/<言語の英語名小文字>.md`（例: 日本語 → `rules/japanese.md`）を
  探す。**あれば Read し、その言語別規則を普遍規則に加えて適用する**。
- 言語別規則ファイルが無い言語は、**普遍規則のみで整形するのが正規の基本動作**
  （縮退ではない）。その場合は、結果の提示に「<言語>の言語別規則は未収録のため、
  普遍規則のみで整形した」と一言明記する。
- 収録済みの言語別規則: 日本語（`rules/japanese.md`）。
- 他のスキルが本スキルを参照利用する場合も、本節に従って言語別規則ファイルを解決する。

## 入力

1. ユーザーが `/plainify` に続けて貼り付けたテキスト。
2. ファイルパス指定（`@path` や「ファイル: path」）。指定があればその内容を読む。
   ファイルは読み取りにのみ使い、整形結果はテキストで返す（ファイルへの反映は
   ユーザーに委ねる）。
3. GitHub の PR / issue（URL または #番号）。gh CLI で本文を取得して対象にする
   （読み取り専用: `gh pr view` / `gh issue view`。整形結果はテキストで返し、
   GitHub への反映はユーザーに委ねる）。gh が使えなければ本文の貼り付けを案内して待つ。
4. 入力が無ければ「整形したいテキストを貼ってください」とだけ尋ねて待つ。
   直前のセッション出力を勝手に対象にしない（自動傍受はしない方針）。

## 手順

### 1. 言語判定と言語別規則の解決（整形前）

「言語の扱い」に従って主たる言語を判定し、言語別規則ファイルを解決する。
言語別規則が整形前の確認（例: 日本語の文体判定）を規定している場合は、
それに従ってから整形に入る。

### 2. リライト規則

**大原則 — 意味厳守:** 主張・指摘の数／強度／条件／例外／数値／評価語（妥当・適切・
望ましい 等の判断）、および文と文の論理関係（因果・逆接・並列・例示）を
1 つも増減しない・すり替えない。この範囲を守る限り、語順の変更・文の分割や統合・
段落内の並べ替えは行ってよい。

**もう 1 つの原則 — 直しすぎない:** 既に明快な箇所は変えない。改善の必要がなければ
そのまま通す（冗長な書き換えをしない）。迷ったら原文を残す。

規則は次の 4 グループ。番号順に「小さい単位 → 大きい単位」で見ていくと漏れがない。
言語固有の言い回しの型（何がその言語で「硬い・不自然」に当たるか）は言語別規則が定める。

#### 2-1. 語彙・語法 — 言葉を平易にする

- **表現の冗語のみ削除** — 重複、空虚な前置き、冗長な言い回し。情報は削らない。
- **名詞化の硬さを動詞表現へ** — 名詞の塊で硬くなった文を、平易な動詞中心の文へ直す。
- **語注（用語の平易な補足）は、原文・識別子名から確実に導ける範囲のみ可** —
  例 `is_dirty` → 「未保存の変更があるか」。
  ⚠️ **原文に明示されていない事実・仕様の推測や追加は禁止**（ハルシネーション防止）。
  コードベースの知識で補いたくなっても、原文に無い事実は書かない。

#### 2-2. 文 — 一文を読みやすくする

- **長い一文は複数文に分割** — 一文一意に近づける。
- **間接構文を圧縮する** — 抽象名詞や形式的な言い回しを経由した回りくどい表現を、
  直接表現へ直す。
- **省略・曖昧になっている動作主を補う** — 読み手が動作主（主語）を補えない文、
  文をまたいで動作主がすり替わる箇所には、**原文から確実に導ける場合のみ**明示する。
  導けなければそのまま残す。

#### 2-3. 段落・構成 — 流れを分かりやすくする

- **結論を先に出す** — 段落の中心となる主張が末尾に埋まっている場合、段落の先頭へ
  出してよい（根拠→結論 の並びを 結論→根拠 に並べ替える）。主張の数・強度・
  論理関係は変えない。
- **1 段落 1 トピック** — 複数の話題が 1 段落に詰まっていれば段落を分ける。
  同じ話題が細切れになっていれば統合してよい。
- **指示語を解消する** — 指示対象が遠い・曖昧な指示語は、指示対象が
  原文から一意に定まる場合のみ、具体的な語へ置き換える。
- **論理関係を明示する** — 文と文のつながりが読み取りにくい場合、原文の論理から
  確実に導ける接続表現（「そのため」「一方」「たとえば」）を補ってよい。
  ⚠️ 原文に無い論理関係を新しく作るのは禁止。

#### 2-4. 見出し・ラベル・書式

- **見出し・ラベルの文言を平易化してよい**（セクション構造自体は保つ）— 例「概要」→
  「このPRがやっていること」、「総じて」→「まとめ」、「(情報) …」→「補足① …（短い説明見出し）」。
- **ツール固有の採点タグは削ってよい（ただし既定は保持）** — 削除してよいのは、文中に浮いた
  純粋なツール内部ラベル（`CONFIRMED` / `PLAUSIBLE`、「act対象」等、人間の判断に寄与しないもの）に限る。
  **構造化された重要度・確信度・出所ラベル**（例: 重要度見出し、`確信度: 80`、`出所: review/codex`）は
  読み手の判断材料なので**既定で保持する**。指摘の内容・重要度の実体は落とさない。迷ったら残す。
- **段落内でハードラップしない — 1 文 = 1 行、段落間は空行** — Markdown の表示環境には
  soft line break をそのまま改行として描画するものが多い。そこへ英文 `.md` の慣習で
  80 桁前後の物理折り返しを持ち込むと、句読点のない位置で改行される不自然な表示になる。
  段落内は 1 文 = 1 行を原則とし、段落と段落の間だけ空行で区切る。この形なら、改行を
  そのまま描画する環境でも、1 つの段落に連結する環境でも自然に読める。意図的な改行が
  必要な場合に限り、Markdown の hard break（行末の半角スペース 2 つ、または `<br>`）を
  明示的に置く。

### 3. 保護対象（一字一句保持・言い換え禁止）

- コード／識別子／関数名／フック名／クラス名・メソッド名
  （例 `export/format/value`, `route_check(['/users'], …)`, `Exporter::init()`）
- ファイルパス・`file:line`（例 `export-service.php:120,158`）
- 数値・PR / Issue 番号（例 `#482`, `#5`）
- コードブロック（```` ``` ````）とインラインコード（`` ` ``）の中身
- Markdown のセクション構造（見出しの階層、箇条書きの階層、コードブロック）
- 箇条書きの項目の並び順（重要度順・手順など、並び自体が意味を持つもの）
- 段落内の改行位置（原文が 1 文 = 1 行になっている場合はそれを維持する。可読性を理由に
  勝手に物理折り返しを追加しない）

### 4. 内部検証 — 主張の照合パス（出力前に必ず実行・出力には出さない）

1. 原文から「主張・指摘・条件・例外・数値・評価語・文と文の論理関係」を箇条書きで抽出する。
2. 整形後の文と 1 対 1 で突き合わせ、**情報の増減がゼロ**であることを確認する。
3. 並べ替え・分割・統合をした段落について、因果・逆接などの論理関係と、
   指摘と根拠の対応関係が原文どおりであることを確認する。
4. **整形で補った語注・動作主・指示語の置き換え・接続表現・平易化した見出し文言が、
   原文または識別子名から確実に導けることを確認する**。原文に無い事実・仕様・論理を
   足していたら削る（ハルシネーション防止）。
5. 不一致（落ちた条件・増えた事実・変わった強度・原文から導けない補足）があれば、
   整形後の文を直してから提示する。

この照合パスは内部処理であり、結果には出さない。

### 5. 出力

- **整形結果のみ**を返す。before/after や変更点リストは付けない
  （言語別規則が未収録の言語の場合は、「言語の扱い」の規定どおり一言明記を添える）。
- 言語別規則が整形前の確認を規定している場合のみ、整形前に 1 回だけ質問する。

## --for モード（読者適応）

読者を `--for <読者>` で指定された読者に置き換えてリライトする。
既定モードの手順（言語判定 → リライト規則 → 保護対象 → 内部検証 → 出力）を
そのまま土台にし、以下の差分だけを上書きする。差分に無い規則は既定モードのまま
（言語の扱い・出力形式など）。差分のすべての判定は
**「指定読者の判断・理解に必要か」**を基準に行う。

### 大原則 — 事実厳守・詳細度は指定読者に合わせる

主張・指摘の数／強度／条件／例外／数値・件数・重大度は、既定モードと同じく
1 つも増減しない・すり替えない。そのうえで**詳細度だけ**を指定読者に合わせて調整する:

- **落とす**: 指定読者の判断・理解に不要な詳細は落としてよい。
- **足す**: 指定読者が知らないと想定される用語には平易な言い換えか一言の説明を添える
  （例: 非エンジニアの読者に「バリデーション」→「入力内容のチェック」）。
- **捏造禁止**: 原文に無い対象システム固有の事実を書かない。実施・確認していない
  ことを実施済みのように書かない。

### 読者の世界に無いものは落とす

指定読者の世界に存在しないものは drop 対象とする。代表例は、読者が開発の外側にいる
場合の開発の内側 — ローカル環境の情報・開発ツールや AI エージェントの名称・作業の
試行錯誤・セッションの経緯。リライト結果は、それらが読者には存在しない前提で
文章として成立させる。

### 語注の限界 — 一般知識は可・固有事実は不可

一般に確立した技術知識による説明（用語の意味、攻撃手法の一般的な仕組みなど）は、
原文に無くても足してよい。対象システム固有の事実（仕様・挙動・影響範囲）は
既定モードと同じく、原文・識別子名から確実に導ける範囲のみ。

### 保護対象 — keep-or-drop

コード・識別子・パス・`file:line` は、**残すなら一字一句そのまま（keep）、指定読者の
判断に不要なら丸ごと落とす（drop。平易な説明への置き換えも可）**の二択。部分的な
訳し崩し・改変という中間は取らない。数値・件数・PR / Issue 番号・重大度は常に keep。

### 内部検証 — 追加チェック

既定モードの照合パス（手順 4）に次を加える。すべて満たすまで出力しない:

1. drop した情報のそれぞれを「指定読者の判断・理解に不要」の基準で説明できる
   （読者の世界に無いものの drop もこの基準に含める）。
2. 足した説明はすべて一般に確立した技術知識の範囲で、固有事実を足していない。
3. 数値・件数・重大度・主張の数と強度が原文と一致する。

## 較正例の所在

目標とする整形の強さを示す較正用 before / after（既定モード・--for モードとも）は、
**各言語別規則ファイルが持つ**（日本語: `rules/japanese.md`）。言語別規則を適用する
ときは、その較正例を整形の強さの基準にする。
