# agent-skills

コーディングエージェント用の汎用スキルを設計・保守するコンテキスト。スキルが生成する成果物の扱いを議論するための共通語彙を定める。

## Language

**置き場**:
プロジェクト内で特定種別の資料を保管する慣例のディレクトリ（例: 調査レポートの `docs/investigation/`）。置き場の有無はプロジェクトの状態であり、個々の資料を残すべきかどうかとは独立。
_Avoid_: 保存先（置き場は候補の一つにすぎない）

**一時領域**:
プロジェクトの外にあるセッション作業用の場所。ここに生成された資料はリポジトリに痕跡を残さず、提出後の削除作業も不要。
_Avoid_: テンポラリファイル、/tmp（特定環境の実体名）

**保存先確認ゲート**:
ファイルを書く前に、置き場・一時領域・任意パスから保存先をユーザーに散文提示で確認する手順。スキルが承認なしにプロジェクトへディレクトリやファイルを新設しないための関門。

**指摘**:
査読・点検系のスキルが、対象を書き換える前にユーザーへ提示する個々の発見事項。重要度2段階（must: 目的達成や公開を妨げる / should: 品質向上）を持ち、承認の単位となる。
_Avoid_: 改定点、修正点

**承認ゲート**:
ユーザーが承認する指摘を選択するまで、スキルが対象を書き換えない関門。承認の前提として、全指摘がユーザーの読める形で提示済みであることを要求する。

**承認範囲**:
承認ゲートでユーザーが指定する、適用する指摘・提案の範囲（全件・番号指定・適用しない等。選択肢の値集合は各スキルが定める）。スキルの通用範囲3値を指す「適用範囲」とは別語。
_Avoid_: 適用範囲（スキルの通用範囲3値と衝突）

**散文提示**:
ユーザーへの選択・承認・確認を、選択式 UI ツールではなく応答本文の散文で提示し、テキスト回答を待つ提示方式。文面要件の正本はスキル設計規約（AGENTS.md）。
_Avoid_: 選択式確認、選択式 UI での確認

**対話出力**:
操作しているユーザーに読ませる報告・提案・確認。出力言語は対話言語に追従する。二分規則の正本はスキル設計規約（AGENTS.md）の「出力言語の二分規則」。
_Avoid_: 報告（各スキルの節名・工程名と衝突）

**成果物出力**:
対話の外に残り第三者が読む生成物（PR レビュー本文・コミットメッセージ・保存レポート等）。出力言語は対象の既存規約・既存本文の言語に追従し、何を判定材料にするかは各スキルが名指しする。二分規則の正本はスキル設計規約（AGENTS.md）の「出力言語の二分規則」。
_Avoid_: 納品物、最終出力（フェーズ名と紛れる）

**適用範囲**:
スキルがどの範囲で通用すべきかの3値（汎用／ハーネス専用／プロジェクト専用）。汎用は案件・プロジェクト・ハーネスの全軸で通用を要し、ハーネス専用はハーネス軸を、プロジェクト専用はプロジェクト軸を免除する（案件軸はどの適用範囲でも免除しない）。skill-refine はモード別査読基準の、skill-feedback は汎化テストの地平の物差しに使う。パス・内容からの自動確定はせず、引数（`--myself` / `--project`、無指定は汎用）または散文提示の確認で明示する。
_Avoid_: スコープ（多義）、モード（適用範囲を指定する引数の呼称であり、値の集合そのものではない）

**セッション生成物**:
実行中のセッションで作成・変更されたファイル（リポジトリ内外を問わない）。
_Avoid_: 成果物（多義で曖昧）、セッションファイル

**エージェント**:
ハーネス上で動く行為主体（モデルとコンテキストを持つ実行体）。判断・作業・報告と、役割（指揮者・議長・奏者・賢者等）の主語はこちら。サブエージェントは、ハーネスのサブエージェント機構で立てられたエージェント。製品カテゴリとしての「コーディングエージェント」は、ハーネス込みの製品全体を指す慣用として許容する。
_Avoid_: モデル（モデルはエージェントの構成要素）、ハーネス

**ハーネス**:
エージェントを駆動する対話型 LLM CLI ソフトウェア（Claude Code / codex / cursor-agent / agy / grok / hermes 等。実行中の自分が載っているものを含む）。ツール・スキルディレクトリ・サブエージェント機構・承認ダイアログはハーネスに属する。ペインへの委譲先を指すときは「委譲先ハーネス」等と修飾する。TUI ペインでの起用手順の正本は tui-harness。裸のハーネス名（codex / grok 等）はハーネスを指すのが既定で、モデルを指すときは「実効モデル」等で明示する。
_Avoid_: エージェント、CLI ツール

**CLI名**:
ハーネスをカタログ・フラグ値・push ラベル等の識別子文脈で指す機械可読な名前（`catalog.sh` の第一引数。codex / cursor-agent / agy / claude / grok / hermes）。散文の主語には「ハーネス」を使い、引数・ラベルの placeholder には「CLI名」を使う。正本は tui-harness のカタログ。
_Avoid_: ハーネスID、CLI ツール名

**モデル系統**:
モデル名の先頭ファミリートークン（claude / gpt / grok 等）。バージョン・reasoning effort 等は系統に含めず、同系統の別バージョン・別ティアは同席（異系統性なし）として扱う。正本は tui-harness の `catalog.sh family`。
_Avoid_: モデル同一性識別子、モデル系列

**異系統委譲**:
実行中と委譲先のモデル系統を起動前に確定し、異なる系統の組を選ぶ委譲。ハーネスが異なるだけでは成立を保証しない。同系統の許容可否と縮退の規定は利用スキル側が持つ。
_Avoid_: 異ハーネス委譲

**片道委譲**:
ペインへブリーフを送って受理を確認した時点で完了し、回答の回収（応答ファイル・push・pull 安全網・指揮者裁定）を行わない委譲。以後のペインの対話はユーザーに引き渡す。往復（回収まで行う）委譲が既定で、片道は利用スキルが明示したときだけ。正本は tui-harness。
_Avoid_: 送りっぱなし（受理確認まで行う点で異なる）、fire-and-forget

**指揮者**:
タスクを分割・配分し、質問に答え、成果を検収する実行エージェント本体の働き（Claude Code に限らない）。tui-harness の適用ではハーネスへの委譲・承認ダイアログの裁定を担う。maestro はこの働きをセッション構造として固定し、concertino は配役の指揮に用いる。
_Avoid_: オーケストレーター、親エージェント

**裁定スコープ**:
承認ダイアログを許可してよい書き込み先とコマンドの範囲。呼び出し側（利用スキルまたは単発利用時の指揮者）が委譲時に定める。
_Avoid_: 許可リスト、権限スコープ

**指揮者裁定**:
承認ダイアログを pull 安全網の blocked 検知で拾い、裁定スコープと照合して二値裁定する指揮者の働き。

**配役**:
セッション中、特定ロールのタスクを外部エージェントへ割り当てるセッションモードの働き（現行実体: concertino と、その編成1ラッパー solista）。
_Avoid_: キャスティング、ルーティング

**編成**:
セッションに立てる奏者のロールと人数の宣言。奏者合計の上限は4。
_Avoid_: 構成、座組

**奏者**:
配役・委譲においてタスクの実行を担うエージェントのロール名。起用形態は委譲先ハーネスのペイン（concertino）またはサブエージェントか同系統ハーネスのペイン（maestro）。奏者ラベル（`implement-1` 等）がブリーフ・報告・push の識別子になる。
_Avoid_: プレイヤー、ワーカー

**ロール**:
配役の単位。implement（書き込みを伴う実装）・review（批評）・explore（読み取り調査）の3種。
_Avoid_: 役割分担、モード

**パート譜**:
implement 奏者一人に排他的に割り当てられた担当ファイル集合。パート譜の外への書き込みは停止→質問→指揮者の再配分で扱う。
_Avoid_: 担当範囲、スコープ

**single-writer**:
どのファイルも、それへ書ける主体を常に一つに限る規律（編成1では「作業ツリーへ書けるのは一人」という従来形に一致する）。
_Avoid_: 排他制御、ロック

**成果物置き場**:
委譲先ハーネス上のエージェントが書いてよいディレクトリとして、呼び出し側（利用スキル）が委譲時に定める置き場。ブリーフ・回答/報告ファイルの受け渡しに使い、後始末の時期と方法は利用スキルの規定に従う（例: 配役ではプロジェクト直下の `.concertino/<奏者ラベル>/`）。
_Avoid_: 一時領域、スクラッチ領域

**確定待ち**:
推測の sleep ではなく、`mux.sh wait-output` や `mux.sh agent-wait` のような状態観測の成立をもって次へ進む待ち方。タイムアウトは「観測されなかった」という確定情報として扱う。
_Avoid_: sleep 待ち、固定秒の待機

**賢者**:
合議において回答を担うエージェントのロール名（議長系統の固定席 `host` 1席＋起動ごとにハーネスカタログから選ぶ2席）。起用形態は委譲先ハーネスのペインまたはサブエージェント。異系統であることが価値の源泉で、脱落時に他席や議長が代行しない。正本は roundtable。
_Avoid_: パネリスト、メンバー

**議長**:
roundtable で合議の主題に自答せず、ブリーフ配布・回答の突き合わせ・収束判定・最終統合のみを行う実行エージェント本体の働き（Claude Code に限らない）。作業を配分・検収する「指揮者」とは区別する。
_Avoid_: モデレーター、ファシリテーター

**収束判定**:
合議の各ラウンド後に行う二値判定。全賢者に立場変更・未解消の反対・新規論点が無ければ収束とし、残ラウンドを待たず最終統合へ進む。
_Avoid_: 合意形成チェック、コンセンサス判定

**自動承認**:
承認ゲートでユーザーの返信の代わりに、起動時に指定した承認範囲（推奨承認範囲・must のみ・全件）を機械的に採用し、周回上限まで査読ループを無人で続ける承認方式。指摘一覧の本文提示は省かず（提示と承認の結合を保つ）、人が検討しなかった指摘は「未検討見送り」として台帳に残す。安全・曖昧さの確認（git 管理外・対象の多重解決・取り違えガード等）は自動承認の対象外。正本は skill-refine。
_Avoid_: 自動モード（モードは適用範囲の引数呼称）、無人モード、auto

**査読収束**:
査読ループの終了条件。周回の集計に査読台帳の判定済みと等価な再提示が無く、かつ新規の過半数指摘に must が無い状態（正本は skill-refine）。roundtable の「収束判定」（合議の終了判定）とは別語。
_Avoid_: 収束判定（roundtable の合議用語と衝突）

**別席相談**:
実行中と別系統のモデル1席へ、セッションの文脈（問い・手元の案・決定事項・却下事項・参照ファイル）を添えて問いを投げ、以後の対話をそのペインでユーザー自身に引き渡す軽量な相談。実行エージェントは回答を回収も提示もしない（片道委譲）。前回のペインが生きていれば、前回ブリーフからの差分だけを添えて追問できる（続用）。合議とは主題（案の審査か、問いへの独立回答か）と工程との結び付き（固定位置か、非依存か）で区別する。正本は sidebar。
_Avoid_: セカンドオピニオン、簡易合議、素通し（旧: 回答を持ち帰る往復モデルの語）

**指摘ID**:
統合レビューの各指摘に付く「重要度接頭辞＋階層内連番」の識別子（B1 / H7 / M3 / L12）。deep-pr-review が付与し、直近の統合レビュー出力内でのみ有効。選別の指示子として pr-comment が解決する。
_Avoid_: 指摘番号、通し番号

**選別**:
レビュー指摘のうち外部へ投稿する対象をユーザーが明示的に選ぶ工程。指定の解釈の復唱と確認を経ない投稿は禁止（正本は pr-comment）。
_Avoid_: フィルタリング、絞り込み

**公開整形**:
投稿文面を、前提を知らない初見の読者に成立する平易な文章へ書き換える2段の工程（publish-polish → plainify）。投稿対象を選ぶ「選別」とは別工程。正本は pr-comment 同梱の公開整形契約。
_Avoid_: フィルタ、フィルタリング

**レビュー指摘**:
PR 上でレビュアーが著者へ向けて残した個々のコメント（行アンカーのスレッド、レビュー本文の指摘、会話コメント。人間・bot を問わない）。スキルがユーザーへ提示する「指摘」とは別語で、著者側スキル（pr-respond）の入力になる。
_Avoid_: 指摘（スキルの提示物と衝突）、コメント（返信も含む多義）

**対処判定**:
著者側スキルがレビュー指摘ごとに付ける三値の判定とその工程。**妥当**（事実認識が正しく、修正が PR の目的範囲内で局所的 → 修正して返信）／**不当**（事実誤認・PR の目的範囲外・明示済みの意図に反する → 理由を返信）／**要判断**（事実は正しいがトレードオフの選択・目的範囲を超える規模・レビュアーへの質問が要る → 返信せずユーザーへ報告）。最終決定は承認ゲートでユーザーが持ち、上書きできる。正本は pr-respond。
_Avoid_: 妥当性チェック、トリアージ

**修正検証**:
投稿済みの指摘コメントが指す問題が PR の新しい head で解消されているかを検証し、判定つきの返信で追跡を完結させる工程（正本は pr-recheck）。セッションを跨ぐ前提のため、入力は PR 上のコメントとそのメタデータのみに依る。
_Avoid_: 再レビュー、フォローアップ

**解消**:
修正検証の合格判定。指摘した問題が新 head のコードで再現しない状態。部分的な修正は解消に含めず、満たないものはすべて未解消として残る問題の理由を添える。
_Avoid_: 対応済み、修正済み

**GREEN**:
レビューループの合格判定。指定 severity 以上の指摘がゼロである状態。
_Avoid_: 合格、クリア、pass

**レビュー契約**:
レビュー1回分の依頼（対象・観点・却下履歴・保留一覧）と応答（severity 付き指摘列挙または GREEN）の固定形式。正本は iterate-review。
_Avoid_: レビューインターフェース、レビュー形式

**保留**:
指摘として有効だが GREEN 閾値未満のため、そのループでは修正対象外とする扱い。却下（指摘が誤り）とは区別する。
_Avoid_: スキップ、未対応
