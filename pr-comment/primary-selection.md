# pr-comment 一次選択（選別指定なしの起動のみ）（正本）

SKILL.md「一次選択」節のポインタから読まれる手順の正本（適用条件と完了条件は SKILL.md 側が持つ）。

指摘ID・重要度・一行要約だけの候補一覧を、切り捨てず全件、散文提示して投稿したい指摘ID の回答を
待つ（属性がこの3点だけなので、各項目は見出し行1行のブロックでよい。この時点ではアンカー解決・
既存コメント突合をまだ行わない — 下調べの量を実際に投稿する件数に比例させるため）。対象レビューで LOW が丸められて
いる場合は、SKILL.md「丸め済み LOW の解決」の規定に従って丸め前の個別指摘を指摘ID 単位で並べ、
解決不能な指摘ID はその旨を一覧に明示する。回答の解釈は
SKILL.md「選別指定の解釈」の記法に従う。
