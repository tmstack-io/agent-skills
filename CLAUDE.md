# agent-skills — 自作スキル正本リポジトリ

自作 Claude Code スキルの正本。`~/.claude/skills/` の同名エントリはここへのシンボリックリンクであり、どちらのパスを編集しても同じ実体に反映される。新しいマシンへの展開手順と収録一覧は README.md を参照。

## 編集時の前提

- 新規スキルは本リポジトリ直下に `<スキル名>/SKILL.md` として作成し、`ln -s` で `~/.claude/skills/` にリンクを張る。
- スキルを追加・改名・削除したら README.md の収録スキル表を更新する。
- スキルの査読・洗練には `/skill-refine` を使う（点検→承認→修正の2段ゲート）。

## スキル設計規約（本リポジトリの全スキル共通）

- **自己完結**: 依存してよいのは Claude Code 本体機能・標準ツール・外部 CLI（codex 等、存在チェックとフォールバック付き）のみ。プラグインやリポジトリ外のスキルへの参照は書かない。
  - 例外: **リポジトリ内の他スキルへの参照は可**（配布単位はリポジトリ全体とみなす）。形式は「`~/.claude/skills/<名前>/SKILL.md` の規則を読み、このコンテキスト内で適用する」とし、Skill ツール経由の別呼び出しにはしない。現行例: deep-pr-review / smart-commit / client-docs → clarify-ja。
- **起動方式の選択**: 自然文の依頼で発火させたい・他スキルから到達される → モデル起動型（description に「何をするか＋トリガー1分岐1つ」）。手動専用・起動＝実行許可となる重い操作 → `disable-model-invocation: true`（description はヒト向け1行要約、トリガー羅列なし）。
- **出力の安定性**: 各ステップに done / not-done を判定できる完了条件を付ける。曖昧な判断ゲートは二値化する（例: codex-review-loop の GREEN）。網羅が重要な箇所は「全項目が〜」の形で漏れゼロを要求する。
- **安全ゲート**: 破壊的・不可逆な操作は dry-run 既定＋承認ゲート。フォールバックした場合は代替であることを報告に明記する（実施済みと詐称しない）。
- **git への態度を明示する**: ループ・自動修正系のスキルは、git 履歴に触れるか否かを必ず規定する（明示しないと実行側がコミット等の履歴操作を発明する）。あわせて非 git 環境での挙動（対象確認・バックアップ等）も定義する。現行例: codex-review-loop の「git 規律」。
- **単一正本**: 同一の意味を複数箇所に書かない。再掲が必要なら正本を1箇所決めて参照する。サブエージェントへ転記する判定基準は `BEGIN/END` マーカーで区切り「一字一句そのままコピー」と指示する（例: decruft）。
- **肯定形で書く**: 目標挙動を述べ、禁止形は肯定形で表せないハードガードレールに限る（その場合も代替行動を併記）。
- **環境・プロジェクト非依存**: 特定マシンのパス・特定プロジェクトの固有知識を書かない。プロジェクト固有情報は「実行時に CLAUDE.md / rules / package.json 等から収集する手順」として書く。
