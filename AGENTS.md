# agent-skills — コーディングエージェント用汎用スキルの正本リポジトリ

コーディングエージェント用の**汎用**スキルの正本。基本のベース運用は Claude Code（CC）で、`~/.claude/skills/` に各スキルディレクトリへのシンボリックリンクを張って利用する（張り方は README.md を参照）。この構成ではどちらのパスを編集しても同じ実体に反映される。加えて `npx skills`（vercel-labs/skills）による per-skill コピー配布と、codex 等の他エージェントでの利用を前提とする。**CC 側の動作保証が最上位制約**（汎用化のために CC での利便性・確実性を落とさない）。CC 専用スキルは本リポジトリに置かず、claude-skills リポジトリ（`tmstack-io/claude-skills`）に置く。導入手順と収録スキル一覧は README.md を参照。

## 編集時の前提

- 新規スキルは本リポジトリ直下に `<スキル名>/SKILL.md` として作成し、動作確認のため実行エージェントのスキルディレクトリ（CC では `~/.claude/skills/`）へシンボリックリンクを張る。
- スキルを追加・改名・削除したら README.md の収録スキル表を更新する。
- スキルの査読・洗練には `/skill-refine` を使う（点検→承認→修正の2段ゲート。汎用スキルは既定基準、特定エージェント専用スキルはそのエージェント上で `--myself`）。
- 設計セッションで確定した横断用語（複数スキル・複数セッションで使う語彙）はリポジトリ直下の `CONTEXT.md`（グロッサリ。実装詳細は書かない）に記録する。無ければ最初の用語確定時に作成する。覆しにくく・理由が自明でなく・実際のトレードオフの結果である設計判断は `docs/adr/` に ADR として残す（3条件が揃わなければ作らない）。

## スキル設計規約（本リポジトリの全スキル共通）

- **自己完結**: 依存してよいのは実行エージェントの本体機能・標準ツール・外部 CLI（codex 等、存在チェックとフォールバック付き）のみ。プラグインやリポジトリ外のスキルへの参照は書かない。
  - 例外: **リポジトリ内の他スキルへの参照は可**。参照は「対象 SKILL.md を Read し、このコンテキスト内で規則を適用する」形とし、Skill ツール経由の別呼び出しにはしない。現行例: smart-commit / impact-investigation（`--for`）→ plainify。
  - 例外: **外部配布スキルへの依存は、前置きゲート付きに限り可**。作業開始前の存在チェックと、配布元を明記した復旧手順（`npx skills add <owner>/<repo>` 等）を案内して中止するゲートを SKILL.md 自身が備える場合に限り、リポジトリ外のスキルを正本として参照してよい。参照形はリポジトリ内参照と同じ「Read して同一コンテキストで適用」とする。現行例: skill-refine → writing-great-skills（`mattpocock/skills`）。
- **他スキル参照の解決順と前置きゲート**: 配布は per-skill コピー（`npx skills add` はスキルディレクトリ単体をコピーし、同梱リポジトリの他ファイルは配布されない）を前提とする。参照先 SKILL.md は次の順で解決する — (1) **自スキルの隣**（`../<名前>/SKILL.md`。依存先が同じ経路で導入されていればここで解決し、per-skill コピー・symlink 運用・リポジトリ直読みのすべてで成立する）、(2) **実行エージェント自身のスキルディレクトリ**をプロジェクト側 → グローバル側の順（npx skills＝vercel-labs/skills が定義するインストール先。Claude Code: `.claude/skills/` → `~/.claude/skills/`、Codex: `.agents/skills/` → `~/.codex/skills/`。他のエージェントも自身の2箇所を同順で見る）。**他エージェント用のディレクトリは探さない**（実行エージェントに導入されていない依存は欠落として扱う）。依存スキルの存在チェックは作業開始前に行い、見つからなければ**黙って縮退せず、復旧手順（`npx skills add tmstack-io/agent-skills` 等）を案内して何も作業せずに中止**する。復旧手順・フォールバックの案内文言は各 SKILL.md が自前で持つ（per-skill コピーではリポジトリ同梱の共有文書に依存できないため。単一正本の例外として明示的に許容）。
- **ツール名は能力主体＋CC 名の例示**: エージェント固有のツール名を裸で指示しない。「Web 検索できる環境では検索で確認し、できなければ情報不足と明記する（Claude Code では WebSearch）」の形で、能力を主語にし CC でのツール名を例示する（CC 側の指示精度を保つため固有名は残す）。WebFetch / Agent 等も同型。ユーザーへの選択・承認・確認の提示は次項の散文提示に従う。
- **選択肢は散文で提示する（散文提示）**: ユーザーに選択・承認・確認を求めるときは、選択肢を応答本文に散文で提示し、テキストでの回答を待つ。選択式 UI ツール（Claude Code の AskUserQuestion 等）は使わない（ツールが呼ばれず選択肢が提示されないままターンが終わる機能漏れを防ぐ）。文面は次のすべてを満たす:
  - 選択肢どうしを空行で区切り、各選択肢に番号または短い見出しを付け、推奨案があれば理由一言つきで明示する
  - 例外: 定型の選択肢で、どれも一行で意味が通る場合は、空行区切りにせず一行に畳んでよい（推奨案と理由の明示は維持する）。現行例: skill-refine の適用範囲確認、decruft の対象確認・適用範囲確認
  - 読者想定は「対象物の作者本人だが、査読・分析の内部用語は知らない人」。各選択肢は「現状どうなっているか → 採用すると何がどう変わるか」が再読なしに分かる完結した文で書く（体言止めの連結・記号連結で圧縮しない）
  - プロセス内部の術語・ラベルを文面に使う場合は、その場で一言の説明を添える
- **サブエージェントは加速オプション**: 並列サブエージェントが並列化・コンテキスト保護の手段にすぎないスキルでは、本文を自己実行の手順として書き、「並列サブエージェントが使える環境（Claude Code の Agent ツール等）では〜してよい」を付記する。独立コンテキストの多角性そのものが品質の源泉であるスキルは CC 専用として claude-skills リポジトリに置く（例: 同リポジトリの deep-pr-review）。
- **起動方式の選択**: 自然文の依頼で発火させたい・他スキルから到達される → モデル起動型（description に「何をするか＋トリガー1分岐1つ」）。手動専用・起動＝実行許可となる重い操作 → `disable-model-invocation: true`（description はヒト向け1行要約、トリガー羅列なし）。汎用スキルで `disable-model-invocation: true` を使う場合は、このフィールドを解釈しない実行環境向けに、本文冒頭へ「明示起動の確認」ゲート（名指し起動時のみ実行。文脈からの自動適用で読み込まれた場合は実行せず起動意思を確認）を置く。
- **frontmatter は CC 仕様を正とする**: `disable-model-invocation` / `argument-hint` は他エージェントでは無視されるだけで害はなく、削ると CC 側の挙動が壊れるため維持する。
- **他エージェント向けメタデータは `agents/<エージェント名>.yaml`**: 全スキルに `agents/openai.yaml` を置き、`interface.display_name` / `interface.short_description` を記載する。`disable-model-invocation: true` のスキルはさらに `policy.allow_implicit_invocation: false` を併記し、このフィールドを解釈しないエージェントでも暗黙起動を抑止する。新規スキル作成時に必ず用意する。
- **出力の安定性**: 各ステップに done / not-done を判定できる完了条件を付ける。曖昧な判断ゲートは二値化する（例: 「問題がなければ」→「指摘ゼロなら」）。網羅が重要な箇所は「全項目が〜」の形で漏れゼロを要求する。
- **安全ゲート**: 破壊的・不可逆な操作は dry-run 既定＋承認ゲート。フォールバックした場合は代替であることを報告に明記する（実施済みと詐称しない）。
- **git への態度を明示する**: ループ・自動修正系のスキルは、git 履歴に触れるか否かを必ず規定する（明示しないと実行側がコミット等の履歴操作を発明する）。あわせて非 git 環境での挙動（対象確認・バックアップ等）も定義する。現行例: smart-commit の [0] 前提チェックと decruft の git 前提チェック。
- **単一正本**: 同一の意味を複数箇所に書かない。再掲が必要なら正本を1箇所決めて参照する。サブエージェントへ転記する判定基準は `BEGIN/END` マーカーで区切り「一字一句そのままコピー」と指示する（例: decruft）。他スキル参照の解決順・復旧手順の文言だけは上記のとおり各スキルが自前で持ってよい（判定基準・手順の実体は重複させない）。
- **肯定形で書く**: 目標挙動を述べ、禁止形は肯定形で表せないハードガードレールに限る（その場合も代替行動を併記）。
- **環境・プロジェクト非依存**: 特定マシンのパス・特定プロジェクトの固有知識を書かない。プロジェクト固有情報は「実行時に CLAUDE.md / rules / package.json 等から収集する手順」として書く。実行環境のグローバル設定に言及する場合は環境ごとの実体を例示する（Claude Code: `~/.claude/CLAUDE.md`、Codex: `~/.codex/AGENTS.md` 等）。
