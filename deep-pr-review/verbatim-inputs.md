# verbatim-inputs — 逐語ペイロードの正本（deep-pr-review 同梱）

SKILL.md の各フェーズがサブエージェント・外部ハーネスへ**逐語で渡す**ペイロードの正本。
節見出しは SKILL.md からの参照キーであり、改名しない。本文は逐語で渡す素材であり、渡す際に
要約・言い換えを経由させない。どの節を誰に渡すかは SKILL.md の各フェーズの記述が正本である
（本ファイルには渡し先の対応を持たない）。

## 確信度ルーブリック

- **0**: 全く確信なし。軽い精査で崩れる偽陽性、または既存からの問題
  （「偽陽性として扱う例」の未変更行例外に該当する場合を除く）。
- **25**: ある程度。実問題かもしれないが偽陽性の可能性もあり、検証しきれていない。様式の問題なら関連する規約文書（CLAUDE.md / AGENTS.md 等）に明記されていないもの。
- **50**: 中程度。実問題と確認できたが、nitpick か発生頻度が低い。PR 全体の中では重要度は高くない。
- **75**: 高い。再確認の上、実運用で踏む実問題である可能性が非常に高い。現状の実装は不十分。または関連する規約文書（CLAUDE.md / AGENTS.md 等）に直接記載がある問題。
- **100**: 確実。実問題で頻繁に発生すると確認済み。証拠が直接それを裏付ける。

## 重要度ルーブリック

- **BLOCKER**: 確実なバグ・データ損失・セキュリティ欠陥など、マージ前に必ず直すべき重大問題。
- **HIGH**: 高確信の実バグ。実運用で踏む可能性が高く、現状の実装では不十分。
- **MEDIUM**: 実害はあるが頻度が低い／影響が限定的。
- **LOW**: nitpick・様式・低確信。直すと良いが必須ではない。

## 偽陽性として扱う例

- 既存（PR 変更前）から存在する問題（本リスト最終項の未変更行例外に該当する場合を除く）。
- バグに見えるが実際はバグでないもの。
- シニアエンジニアが指摘しない過度な nitpick。
- linter / typechecker / compiler が拾う類（import 漏れ・型エラー・フォーマット）。これらは CI が別途実行する前提で、自分でビルド/型チェックはしない。
- 一般的なコード品質（テスト網羅・ドキュメント不足等）。ただし規約文書（CLAUDE.md / AGENTS.md 等）が明示要求している場合は除く。
- 規約文書に記載があるがコード側で明示的に抑制されている（lint ignore 等）もの。
- 意図的・本変更に直接関連すると見られる挙動変更。
- 実問題だが、ユーザーが PR で変更していない行に対するもの。ただし、PR の変更が当該問題の
  露出を広げる場合、または同じデータソースを読み書きする対称実装との非対称を PR が新たに作る
  場合は、未変更行でも対象内とする。
